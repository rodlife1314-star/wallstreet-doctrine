# Deployment Templates & Calibration Profiles

This directory holds reusable configuration matrices, profile templates, and conversion calibrations.

## Components
- `engine_build.sh`: High-level build sequence wrapping local shell compilation routines.
- `calibration_dataset.json`: Reference datasets optimizing Activation Ranges during INT8 compilation maps.
