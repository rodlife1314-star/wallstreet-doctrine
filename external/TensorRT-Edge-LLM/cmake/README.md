# CMake Configuration Modules

This directory contains CMake find-package configurations, target declarations, and dynamic library-discovery logic.

## Modules
- `FindTensorRT.cmake`: Dynamic detection of local TensorRT shared library footprints during build runtime configuration.
